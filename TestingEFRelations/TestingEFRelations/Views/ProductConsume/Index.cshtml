@model IEnumerable<TestingEFRelations.Models.Product>


@{
    ViewData["Title"] = "Consume Main Menu";
}

<h1>API Consume Main Menu</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<div class="row">
    <a asp-controller="AccountConsumer" asp-action="SignInApi"  class="btn btn-info">SignIn</a>
</div>

<div class="row">
    @if(Model?.Count() > 0){
    
        @foreach (var item in Model)
        {

            <div class="card m-1" style="width: 18rem; ">

                <div class="card-body">
                    <a asp-action="Details" asp-route-id="@item.ProductID"><img src="@item.ProductImage[0].ImageName" class="card-img-top" width="50" height="150" alt="@item.ProductName"></a>

                    <a asp-action="Details" asp-route-id="@item.ProductID"><p class="card-title text-center text-dark">@Html.DisplayFor(modelItem => item.ProductName)</p></a>

                    <p class="card-text">@Html.DisplayFor(modelItem => item.ProductDescription)</p>

                    <p class="card-text">@Html.DisplayFor(modelItem => item.ProductPrice)$</p>

                    |<a asp-action="Edit" asp-route-id="@item.ProductID">Edit</a>

                </div>
            </div>

         }



    }
    else
    {
        @ViewData["errorMessage"];
    }
</div>